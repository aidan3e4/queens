Solver for the Linkedin game Queens.

Start the script with `python main.py`, which will then start monitoring the screen. Once a grid is detected on the screen, a solution will be displayed.

# TODO:
- improve cells detection algo (doesn't work well on screen shots for example)